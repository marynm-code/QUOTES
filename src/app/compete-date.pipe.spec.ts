import { CompeteDatePipe } from './compete-date.pipe';

describe('CompeteDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CompeteDatePipe();
    expect(pipe).toBeTruthy();
  });
});
